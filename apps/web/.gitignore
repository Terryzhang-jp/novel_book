# Dependencies
node_modules
.pnp
.pnp.js

# Testing
coverage

# Next.js
.next/
out/
build
dist

# Production
.vercel

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Local env files
.env.local
.env*.local
.env.development.local
.env.test.local
.env.production.local

# Turbo
.turbo

# 本地数据存储（不要提交到 git）
data/

# 上传的图片（不要提交到 git）
public/images/*
!public/images/.gitkeep

# ⚠️ 这两条曾经把「数据库 schema」整个挡在版本控制之外，导致仓库无法从零
#    重建数据库（PERFORMANCE-AUDIT.md Q14）。已扫描确认其中无硬编码凭据，
#    现纳入版本管理。
#
# 另注：原来的 `supabase/` 没有前导斜杠，会匹配任意层级的同名目录 ——
# 包括存放生产代码的 lib/supabase/。已改为锚定到 apps/web 根。
#
# scripts/     ← 已纳管
# supabase/    ← 已纳管

# 部署脚本（包含敏感信息）
deploy-to-vercel.sh

# 内部文档
MIGRATION_INSTRUCTIONS.md
SUPABASE_MIGRATION.md
MINIMAL_DESIGN.md
PHOTO_EDITOR_OPTIMIZATION.md
perf-results/

# 新核心的本地对象存储（ADR-002：必须在 public/ 之外）
.storage/
